#include <stdio.h>
#include <string.h>
int i,j,k,r,len1,len2;
char n1[100],n2[100];
int main(){
    scanf("%s",n1);
    scanf("%s",n2);

    len1 = strlen(n1);
    len2 = strlen(n2);

int c =0;

    for(r=1;r<=4;r++){
        //// หาที่ไม่ซ้ำ
        for(i=0;i<len1;i++){
            for(j=0;j<len2;j++){
                if(n1[i])
            }
        }





    }
}